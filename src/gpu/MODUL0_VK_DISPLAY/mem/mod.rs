//! Memory layer — base/{embedded,transport} + generator (owner MODUL0_VK_DISPLAY).
pub mod base;
/// Submodule `asm_disasm`.
/// Part of `gpu/MODUL0_VK_DISPLAY/mem` under the mem/conv/proc MCG canon.
pub mod asm_disasm;
