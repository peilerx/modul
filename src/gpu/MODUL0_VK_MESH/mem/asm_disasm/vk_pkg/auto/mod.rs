//! Rank **`vk_pkg/auto`** — mesh Auto packs (device assemble · PortMatch).
//! DomainMath peels (SoA · push · center/radius) live in `proc/processor`.

pub mod line_gpu_res_intsct_at_asm;
pub mod mesh_draw_prt_at_asm;
