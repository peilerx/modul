//! Memory layer — bags (`base/`) and assembler cargo (`asm_disasm/`).

pub mod base;
/// Submodule `asm_disasm`.
/// Part of `gpu/MODUL0_VK_MESH/mem` under the mem/conv/proc MCG canon.
pub mod asm_disasm;
