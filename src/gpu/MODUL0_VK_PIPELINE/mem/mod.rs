//! Memory layer — base/{embedded,transport} + generator (owner MODUL0_VK_PIPELINE).
pub mod base;
/// Submodule `asm_disasm`.
/// Part of `gpu/MODUL0_VK_PIPELINE/mem` under the mem/conv/proc MCG canon.
pub mod asm_disasm;
