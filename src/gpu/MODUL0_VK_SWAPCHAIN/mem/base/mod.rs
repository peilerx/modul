//! Memory base — **embedded** (Internal-only) ∧ **transport** (shareable W).
pub mod embedded;
/// Submodule `transport`.
/// Part of `gpu/MODUL0_VK_SWAPCHAIN/mem/base` under the mem/conv/proc MCG canon.
pub mod transport;
