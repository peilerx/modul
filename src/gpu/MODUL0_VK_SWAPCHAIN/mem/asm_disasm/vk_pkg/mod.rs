//! Rank **vk_pkg** — `*Pkg` / embedded atom bags; may call **vk** (FIX-120).

pub mod auto;
/// Submodule `handled`.
/// Part of `gpu/MODUL0_VK_SWAPCHAIN/mem/asm_disasm/vk_pkg` under the mem/conv/proc MCG canon.
pub mod handled;
