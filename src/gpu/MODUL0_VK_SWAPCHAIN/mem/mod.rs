//! Memory layer — base/{embedded,transport} + asm_disasm ranks (incl. vk_bfr).
pub mod base;
/// Submodule `asm_disasm`.
/// Part of `gpu/MODUL0_VK_SWAPCHAIN/mem` under the mem/conv/proc MCG canon.
pub mod asm_disasm;
