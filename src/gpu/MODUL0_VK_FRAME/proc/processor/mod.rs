//! FRAME processor — loops / domain control flow only (B34).
//! vk `CreateInfo` + create_* live in gen (A1-GEN-MATERIALIZE).

pub mod frame_sync;
/// Submodule `frame_tick`.
/// Part of `gpu/MODUL0_VK_FRAME/proc/processor` under the mem/conv/proc MCG canon.
pub mod frame_tick;
