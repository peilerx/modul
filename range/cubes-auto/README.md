# range/cubes

Direct Vulkan cubes etalon on **modul** (T.Hub · Slot-Factory-Line · no Viewsor).

```bash
cd modul-project
cargo run -p cubes --release
# optional load: CUBES_COUNT=1000 cargo run -p cubes --release
```

Controls: LMB orbit · wheel zoom · Esc quit.
